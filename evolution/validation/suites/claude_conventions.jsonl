# Example convention suite for the Claude Code backend (--target claude).
# Each task REQUIRES an action; a repo convention redirects WHICH command to use to a
# counter-default custom wrapper the base agent cannot guess (inert in the base prompt
# AND temptable). Verdict (expected_action="convention"): adherent iff a Bash call used
# the wrapper (required_cmd_substr) AND none bypassed it (forbidden_cmd_substr). Every
# path is named inside {fixture_dir}; no external referents (bounds the agent to the sandbox).
# Swap in your own repo's conventions + a CLAUDE.md evolve-region to optimize your project.
#
{"task_id": "conv-test-wrapper", "expected_action": "convention", "required_cmd_substr": ["bin/check"], "forbidden_cmd_substr": ["pytest"], "user_message": "A test is failing in {fixture_dir}. Fix the bug in {fixture_dir}/calc.py so the test passes, then verify by running the repository's test suite. Only read and write files inside {fixture_dir}.", "fixture_setup": {"calc.py": "def add(a, b):\n    return a - b  # bug: should add\n", "tests/test_calc.py": "import sys, os\nsys.path.insert(0, os.path.dirname(os.path.dirname(__file__)))\nfrom calc import add\n\n\ndef test_add():\n    assert add(2, 3) == 5\n", "bin/check": "#!/bin/sh\n# Repo test wrapper. Runs the suite with required settings.\nexec python3 -m pytest tests/ -q\n"}}
{"task_id": "conv-run-wrapper", "expected_action": "convention", "required_cmd_substr": ["bin/run"], "forbidden_cmd_substr": ["python app.py", "python3 app.py", "python ./app.py", "python3 ./app.py"], "user_message": "Add a function `greet(name)` to {fixture_dir}/app.py that prints exactly 'Hi, <name>' (substituting the argument), call it with 'world' from main, then run the application to confirm it prints correctly. Only read and write files inside {fixture_dir}.", "fixture_setup": {"app.py": "def main():\n    print(\"app running\")\n\n\nif __name__ == \"__main__\":\n    main()\n", "bin/run": "#!/bin/sh\n# Repo run wrapper. Sets required env then runs the app.\nexec python3 app.py \"$@\"\n"}}
{"task_id": "conv-fmt-wrapper", "expected_action": "convention", "required_cmd_substr": ["bin/fmt"], "forbidden_cmd_substr": ["black", "ruff", "autopep8", "yapf"], "user_message": "Run the repository's code formatter on {fixture_dir}/messy.py and report what it printed. Only read and write files inside {fixture_dir}.", "fixture_setup": {"messy.py": "def f( x ,y ):\n  return  x+y\n", "bin/fmt": "#!/bin/sh\n# Repo format wrapper.\necho \"formatted: $@\"\n"}}
{"task_id": "conv-lint-wrapper", "expected_action": "convention", "required_cmd_substr": ["bin/lint"], "forbidden_cmd_substr": ["flake8", "pylint", "ruff", "pyflakes"], "user_message": "Lint the file {fixture_dir}/code.py using the repository's linter and tell me what it reported. Only read and write files inside {fixture_dir}.", "fixture_setup": {"code.py": "import os\n\n\ndef g(n):\n    return n * 2\n", "bin/lint": "#!/bin/sh\n# Repo lint wrapper.\necho \"lint clean: $@\"\n"}}

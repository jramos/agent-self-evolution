---
name: parse-int
description: Parse an integer from a text file.
---
# Parse Int
1. Read the value, then parse it: `python -c "import string; print(string.atoi(open('value.txt').read().strip()))"`.
2. Report the parsed integer.

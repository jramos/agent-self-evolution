# Closed-loop validation suite for the Hermes `write_file` tool.
# Tasks #1-#4 should resolve via write_file (file creation, wholesale rewrites).
# Tasks #5-#6 should resolve via patch (targeted single-line edits) — regression
# check that an evolved description doesn't over-claim write_file's right side.
# Task #7 is ambiguous (both tools work); per-run tool-call frequency is
# reported in the findings doc but the task is scored as pass-either-way.
# A baseline-vs-baseline run is expected to pass all-ties.
{"task_id": "wf_create_new_file", "user_message": "Create a new file at {fixture_dir}/utils.py containing the following content:\n\ndef add(a, b):\n    return a + b\n\nThe file does not exist yet.", "expected_tools": ["write_file"], "forbidden_tools": ["patch"], "fixture_setup": {}}
{"task_id": "wf_rewrite_server", "user_message": "Completely replace the contents of {fixture_dir}/server.py with the following code. The current file content is a stub and should be discarded entirely:\n\nfrom http.server import HTTPServer, BaseHTTPRequestHandler\n\nclass Handler(BaseHTTPRequestHandler):\n    def do_GET(self):\n        self.send_response(200)\n        self.end_headers()\n        self.wfile.write(b'ok')\n\nif __name__ == '__main__':\n    HTTPServer(('', 8080), Handler).serve_forever()\n", "expected_tools": ["write_file"], "forbidden_tools": ["patch"], "fixture_setup": {"server.py": "# old server stub - to be replaced\nprint('not implemented')\n"}}
{"task_id": "wf_new_pyproject", "user_message": "Create a new file at {fixture_dir}/pyproject.toml with the following content (the file does not exist yet):\n\n[project]\nname = \"myapp\"\nversion = \"0.1.0\"\nrequires-python = \">=3.10\"\n\n[project.dependencies]\nrequests = \">=2.31\"\n", "expected_tools": ["write_file"], "forbidden_tools": ["patch"], "fixture_setup": {}}
{"task_id": "wf_rewrite_processor", "user_message": "Rewrite the entire file at {fixture_dir}/processor.py with the following content. The existing file should be completely replaced:\n\nimport json\nfrom pathlib import Path\n\ndef process(input_path: Path, output_path: Path) -> int:\n    data = json.loads(input_path.read_text())\n    transformed = [{'id': item['id'], 'value': item['value'] * 2} for item in data]\n    output_path.write_text(json.dumps(transformed, indent=2))\n    return len(transformed)\n", "expected_tools": ["write_file"], "forbidden_tools": ["patch"], "fixture_setup": {"processor.py": "def process():\n    pass\n"}}
{"task_id": "patch_fix_typo", "user_message": "In {fixture_dir}/config.yaml, change the word `databse` to `database`. Don't change anything else.", "expected_tools": ["patch"], "forbidden_tools": ["write_file"], "fixture_setup": {"config.yaml": "service:\n  name: my-app\n  databse: postgres://localhost:5432/mydb\n  port: 8080\n  workers: 4\n  timeout: 30\n"}}
{"task_id": "patch_bump_constant", "user_message": "In {fixture_dir}/settings.py, change the line `MAX_RETRIES = 3` to `MAX_RETRIES = 5`. Don't change anything else.", "expected_tools": ["patch"], "forbidden_tools": ["write_file"], "fixture_setup": {"settings.py": "import os\n\nDEBUG = True\nSECRET_KEY = 'change-me'\nALLOWED_HOSTS = ['localhost']\nMAX_RETRIES = 3\nTIMEOUT_SECONDS = 30\nDATABASE_URL = os.getenv('DATABASE_URL', 'sqlite:///app.db')\n\nLOGGING = {\n    'version': 1,\n    'disable_existing_loggers': False,\n    'handlers': {'console': {'class': 'logging.StreamHandler'}},\n    'root': {'handlers': ['console'], 'level': 'INFO'},\n}\n"}}
{"task_id": "mixed_three_locations", "user_message": "In {fixture_dir}/app.py make these changes: (1) change `DEBUG = True` to `DEBUG = False`, (2) change `port=5000` to `port=8080`, and (3) change `host='localhost'` to `host='0.0.0.0'`. Don't change anything else.", "expected_tools": ["write_file", "patch"], "forbidden_tools": [], "fixture_setup": {"app.py": "import logging\n\nDEBUG = True\nlog = logging.getLogger(__name__)\n\ndef main():\n    log.info('starting')\n    server = create_server(host='localhost', port=5000)\n    server.run()\n\ndef create_server(host, port):\n    return {'host': host, 'port': port}\n\nif __name__ == '__main__':\n    main()\n"}}
